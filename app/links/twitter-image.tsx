// Twitter card = the same links card as og:image (clients that honor only
// twitter:image would otherwise miss it).
export { default, alt, size, contentType } from './opengraph-image'

export const runtime = 'nodejs'
