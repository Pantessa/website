{
  "framework": "nextjs",
  "buildCommand": "npm run db:generate && next build",
  "crons": [
    {
      "path": "/api/cron/hl-guardian",
      "schedule": "* * * * *"
    },
    {
      "path": "/api/cron/jobs",
      "schedule": "* * * * *"
    }
  ],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
