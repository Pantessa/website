/**
 * The company's X account, in one place. It was @yeetful_ai until the
 * rebrand; X renamed the same account to @askPantessa, and the old handle now
 * 404s (issue #767). Every profile link and every pre-written tweet reads it
 * from here, so a future rename is one edit.
 */
export const X_HANDLE = 'askPantessa'
export const X_MENTION = `@${X_HANDLE}`
export const X_PROFILE_URL = `https://x.com/${X_HANDLE}`
